# 🛠️ Guía rápida de despliegue automático con cron + alias `deploy`

### 1. Edita tu archivo `index.html` en tu PC
- Ruta local:  
  C:\Users\arcom\Panel-n8n\index.html

### 2. Sube el archivo a tu VM desde PowerShell
scp -i $HOME\.ssh\google_compute_engine "C:\Users\arcom\Panel-n8n\index.html" contrerasaurelio67@34.148.6.253:/home/contrerasaurelio67/Panel-n8n/index.html

### 3. Conéctate por SSH si quieres desplegar manualmente
ssh -i $HOME\.ssh\google_compute_engine contrerasaurelio67@34.148.6.253

### 4. Ejecuta el despliegue manual con el alias `deploy`
Una vez dentro de la VM, simplemente escribe:
deploy

Este alias ejecuta el script deploy.sh, guarda el log y muestra las últimas líneas automáticamente.

### 5. Mensaje esperado en el log
Si hubo cambios:
🚀 Comprobando cambios en index.html...
🔄 Cambios detectados. Actualizando...
🎉 index.html actualizado en Nginx.

Si no hubo cambios:
✅ Sin cambios en index.html. No se despliega.

### 6. Despliegue automático con cron (cada 30 min)
Tu VM ejecuta el script automáticamente:
*/30 * * * * /home/contrerasaurelio67/deploy.sh >> /home/contrerasaurelio67/deploy.log 2>&1

### ✅ Resultado
- Si hay cambios → se despliega automáticamente.
- Si no hay cambios → no hace nada.
- Puedes verificar en cualquier momento con:
tail -n 20 /home/contrerasaurelio67/deploy.log

### 🧠 Bonus: alias `deploy` en .bashrc
alias deploy="/home/contrerasaurelio67/deploy.sh >> /home/contrerasaurelio67/deploy.log 2>&1 && tail -n 10 /home/contrerasaurelio67/deploy.log"

# 🧩 Conclusión
Solo necesitas editar tu HTML local y subirlo con scp.
Luego puedes esperar el cron o ejecutar deploy manualmente para ver el resultado al instante.